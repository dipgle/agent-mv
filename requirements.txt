# Python deps for video pipeline orchestrator.
# Tested with Python 3.11 on Win 10/11, macOS 14+, Ubuntu 22.04+.
# Install:  pip install -r requirements.txt

# LLM routing
litellm>=1.55.0
openai>=1.50.0           # used as OpenAI-compatible client → LiteLLM proxy

# Agent orchestration
crewai>=0.86.0
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0

# Audio/Video tooling
ffmpeg-python>=0.2.0     # ffmpeg wrapper (still requires ffmpeg binary on PATH)
soundfile>=0.12.0
librosa>=0.10.0          # BPM detect, audio analysis
pyloudnorm>=0.1.1        # EBU R128 LUFS measurement for audio QC

# Image / vision
Pillow>=10.0.0
opencv-python>=4.10.0    # frame sample for Reviewer

# HuggingFace download (model weights)
huggingface_hub>=0.26.0
hf_transfer>=0.1.8       # faster parallel download

# HTTP
requests>=2.31.0
httpx>=0.27.0

# Data
sqlite-utils>=3.37
pydantic>=2.0.0

# CLI
click>=8.1.0
rich>=13.0.0             # nice CLI output

# Dev/testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.6.0              # linter

# Optional GUI (uncomment if building Tkinter launcher)
# PySide6>=6.7.0
